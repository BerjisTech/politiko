<%= form_with(model: politician) do |form| %>
  <% if politician.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(politician.errors.count, "error") %> prohibited this politician from being saved:</h2>

      <ul>
        <% politician.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :alias %>
    <%= form.text_area :alias %>
  </div>

  <div class="field">
    <%= form.label :current_party %>
    <%= form.text_field :current_party %>
  </div>

  <div class="field">
    <%= form.label :history %>
    <%= form.rich_text_area :history %>
  </div>

  <div class="field">
    <%= form.label :manifesto %>
    <%= form.rich_text_area :manifesto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
